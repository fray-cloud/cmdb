FROM python:3.13-slim AS base
COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv
WORKDIR /app
COPY pyproject.toml uv.lock ./
COPY shared/ ./shared/

FROM base AS prod
RUN uv sync --frozen --no-dev --package cmdb-shared

FROM base AS dev
RUN uv sync --frozen --package cmdb-shared
